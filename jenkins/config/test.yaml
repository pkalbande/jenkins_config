jenkins:
  systemMessage: "Welcome to Jenkins configured with JCasC! *** poi" # Custom system message

  securityRealm:
    local:
      allowsSignup: false
      users:
      - id: "admin"
        password: "admin123"
        name: "Administrator"
